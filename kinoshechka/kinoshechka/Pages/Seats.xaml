<Page x:Class="kinoshechka.Pages.SeatsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:kinoshechka.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800" Background="AliceBlue"
      Title="Seats">
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Button Content="Назад"
                HorizontalAlignment="Left"
                Padding="10,5"
                Click="Back_Click"/>

        <Border Grid.Row="1" Margin="0,10,0,20" Padding="10"
                BorderBrush="Gray" BorderThickness="1">
            <StackPanel>
                <TextBlock x:Name="MovieTitleText" FontSize="18" FontWeight="Bold" Margin="0,0,0,5"/>
                <TextBlock x:Name="SessionDateText"/>
                <TextBlock x:Name="SessionTimeText"/>
                <TextBlock x:Name="HallNameText"/>
                <TextBlock x:Name="HallTypeText"/>
                <TextBlock x:Name="PriceInfoText"/>
            </StackPanel>
        </Border>

        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <ItemsControl x:Name="SeatsItems">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>

                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Button Content="{Binding Seat.SeatNumber}"
                                Width="50" Height="40"
                                Margin="5"
                                Click="Seat_Click"
                                Tag="{Binding Seat}">   
                            <Button.Style>
                                <Style TargetType="Button">
                                    <Setter Property="Background" Value="LightGreen"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsOccupied}" Value="True">
                                            <Setter Property="Background" Value="LightCoral"/>
                                            <Setter Property="IsEnabled" Value="False"/>
                                        </DataTrigger>
                                       
                                        <DataTrigger Binding="{Binding Seat.IsSelected, RelativeSource={RelativeSource AncestorType=ItemsControl}}" Value="True">
                                            <Setter Property="Background" Value="LightBlue"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <Border Grid.Row="3" Margin="0,10,0,0" Padding="10"
            BorderBrush="Gray" BorderThickness="1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" VerticalAlignment="Center">
                    <TextBlock x:Name="SelectedSeatsText" Margin="0,0,0,5"/>
                    <TextBlock x:Name="TotalPriceText" FontWeight="Bold"/>
                </StackPanel>

                <Button Grid.Column="0" 
                        Width="90"
                        Content="Очистить"
                        Padding="15,10"
                        Click="Clear_Click"/>

                <Button Grid.Column="1" 
                        Content="Купить"
                        Padding="20,10"
                        Margin="10,0,0,0"
                        Click="BuyTickets_Click"/>
            </Grid>
        </Border>
    </Grid>
</Page>
           